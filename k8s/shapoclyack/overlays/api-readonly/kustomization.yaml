# Thin results-only API image (shapoclyack-api, no local scan start).
# Prefer overlays/dev for all-in-one (shapoclyack-aio).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: api-readonly-patch.yaml
